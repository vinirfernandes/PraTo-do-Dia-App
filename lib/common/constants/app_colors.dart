import 'dart:ui';

class AppColors {
  AppColors._();

  // Cores de elementos/assets/fundos
  static const Color gradienteClaro = Color(0xFF129575);
  static const Color gradienteEscuro = Color(0xFF053E30);
  static const Color gradienteMedio = Color(0xFF0B6952);

  // Cores de texto
  static const Color textoBranco = Color(0xFFFFFFFF);
  static const Color textoPreto = Color.fromARGB(255, 0, 0, 0);
  static const Color transparente = Color.fromARGB(0, 0, 0, 0);
}
